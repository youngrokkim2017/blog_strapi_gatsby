import React from "react"

const Footer = () => {
    return (
        <nav>
            <div>
                {/* links to about, subscribe, etc */}
            </div>
        </nav>
    )
}

export default Footer;